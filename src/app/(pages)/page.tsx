import { ReactElement } from "react";
import Hero from "@/components/hero";

const LandingPage = (): ReactElement => (
    <main className="mt-52 flex flex-col">
        <Hero />
    </main>
);
export default LandingPage;
